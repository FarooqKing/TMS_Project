@model IEnumerable<TMS_Project.Models.Notification>

@{
    ViewData["Title"] = "Index";
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title">Notifications</h4>
                <a asp-action="Create" class="btn btn-primary">Create New</a>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="example4" class="display table table-striped">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.NotificatonDetails)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.NotificationTo)
                                </th>

                                <th>
                                    @Html.DisplayNameFor(model => model.Task)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NotificatonDetails)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NotificationTo)
                                    </td>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.Task.TaskId)
                                    </td>
                                    <td>
                                        <div class="d-flex">
                                            <a asp-action="Edit" class="btn btn-info btn-sm m-2" asp-route-id="@item.NotificatonId" title="Edit"><i class="fa fa-edit"></i></a>
                                            <a asp-action="Details" class="btn btn-warning btn-sm m-2" asp-route-id="@item.NotificatonId"><i class="fa fa-info"></i></a>
                                            <a asp-action="Delete" class="btn btn-danger btn-sm m-2" asp-route-id="@item.NotificatonId"><i class="fa fa-trash"></i></a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {

    <script type="text/javascript">

    </script>
}