@model TMS_Project.Models.Project

@{

}

<div class="row">
    <div class="col-md-12 col-lg-12 col-sm-12">
        <div class="form-validation">
            <form asp-action="Create" id="CreateProjectForm" class="needs-validation" novalidate>
                <input type="hidden" asp-for="ProjectId" id="hdnProjectId" />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="col-md-6">
                    <label asp-for="ProjectName" class="control-label">Project Name</label>
                    <input asp-for="ProjectName" class="form-control" required />
                    <div class="invalid-feedback">
                        Please enter a Project Name.
                    </div>
                    <span asp-validation-for="ProjectName" class="text-danger"></span>
                </div>
                @*    <div class="form-group">
                <div class="col-md-6">
                <label asp-for="TeamLeadId" class="control-label">Team Lead</label>
                <input asp-for="TeamLeadId" class="form-control"  />
                <div class="invalid-feedback">
                Please enter a Team Lead Id.
                </div>
                <span asp-validation-for="TeamLeadId" class="text-danger"></span>
                </div> *@
                <div class="form-group">
                    <div class="col-md-6">
                        <label asp-for="ManagerId" class="control-label">Manager </label>
                        <select asp-for="ManagerId" class="form-control" asp-items="ViewBag.ManagerId"></select>
                        <div class="invalid-feedback">
                            Please enter Manager Id.
                        </div>
                    </div>

                    <div class="col-md-6">
                        <a id="btnCreateProjectPost" onclick="InsertProject()" class="btn mb-2"></a>
                    </div>
                </div>

            </form>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}